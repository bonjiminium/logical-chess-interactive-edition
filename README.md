# Logical Chess: Move by Move — Interactive Edition

A static interactive edition of selected lessons from *Logical Chess: Move by Move*.

The site is deployed with GitHub Pages. Its public `robots.txt` asks cooperating
search crawlers not to crawl or index the site; it does **not** restrict access
to people who know the URL.
